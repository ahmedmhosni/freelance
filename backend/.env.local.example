# ============================================
# LOCAL DEVELOPMENT ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env.local and update with your local settings

# Environment
NODE_ENV=development

# Local PostgreSQL Database Configuration
PG_HOST=localhost
PG_PORT=5432
PG_DATABASE=roastify
PG_USER=postgres
PG_PASSWORD=postgres

# JWT Configuration (use simple secrets for local dev)
JWT_SECRET=local-dev-jwt-secret-key
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=local-dev-refresh-secret-key
JWT_REFRESH_EXPIRES_IN=30d

# Application Configuration
PORT=5000
FRONTEND_URL=http://localhost:5173

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Rate Limiting (more lenient for development)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=10000

# Session Configuration
SESSION_SECRET=local-dev-session-secret

# Logging
LOG_LEVEL=debug

# Azure Storage (Optional - for testing blob storage locally)
# Leave empty to skip Azure features in development
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_CONTAINER_NAME=

# Azure Communication Services (Optional - for testing emails locally)
# Leave empty to skip email features in development
AZURE_COMMUNICATION_CONNECTION_STRING=
AZURE_COMMUNICATION_SENDER_EMAIL=

# ============================================
# NOTES:
# - This configuration uses local PostgreSQL
# - Azure features are optional for local dev
# - Production uses different env vars (DB_HOST, DB_NAME, etc.)
# ============================================
